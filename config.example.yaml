# Danbooru Manager - Beispielauszug für 1.3.9

filename:
  # Unterstützt:
  # %artist%, %artists%, %character%, %characters%, %copyright%, %series%,
  # %serie%, %general%, %meta%, %tags%, %postid%, %id%, %hash%, %ext%
  pattern: "%artist%_%characters%_%general%_%postid%"
  max_length: 180
  tags_count: 8
  hash_length: 8
  excluded_tags:
    - highres
    - absurdres
    - commentary_request
