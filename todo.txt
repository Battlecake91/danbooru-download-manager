less overweighted ui with advanced-options
configurable llm fetch limit
database managed by one instance - if database is locked, it should wait for the request till unlocked and then save. f.e. while fetching changing the config should be possible
-importer should fetch thumbnails
-renaming files renames every file in the category, not only in the folder
